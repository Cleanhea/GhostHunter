# AGENTS.md

> 이 저장소에서 동작하는 **모든 AI 에이전트**(Claude Code, Codex, Cursor, Copilot 등)의 공통 운영 규약.
> Claude Code 전용 진입점은 [CLAUDE.md](CLAUDE.md), 세부 컨텍스트는 [docs/](docs/README.md).

---

## 1. 이 저장소의 AX 원칙

1. **문서가 단일 진실 원천(SSOT)이다.** 에이전트는 추측 대신 `docs/`를 읽는다. 문서와 코드가 충돌하면 문서를 신뢰하지 말고 **불일치를 보고**한다.
2. **컨텍스트는 라우팅으로 전달한다.** 모든 문서를 읽지 않는다. [CLAUDE.md §2 라우팅 표](CLAUDE.md#2-컨텍스트-라우팅--작업-전-필독-문서)에 따라 필요한 것만 읽는다.
3. **작업의 산출물은 코드 + 갱신된 문서다.** 구조·규약·결정이 바뀌었는데 문서가 그대로면 그 작업은 끝난 게 아니다.
4. **결정은 흔적을 남긴다.** 되돌리기 비싼 선택은 ADR로 기록한다.
5. **불확실하면 멈추고 묻는다.** 특히 게임 디자인 수치와 룰은 사람이 정한다.

---

## 2. 표준 작업 프로토콜

| 단계 | 해야 할 일 | 산출물 |
| --- | --- | --- |
| **1. 로드** | 라우팅 표의 필독 문서 + 관련 기존 코드 확인 | 현재 상태 이해 |
| **2. 정렬** | 요구를 1~3문장으로 재진술, 모호점·`TBD` 질문 | 합의된 범위 |
| **3. 계획** | 변경할 파일과 순서를 나열(2단계 이상 작업 시 필수) | 파일 단위 계획 |
| **4. 구현** | 규약 준수, 기존 스타일 모방, 최소 diff | 코드 |
| **5. 검증** | 컴파일 확인 → 테스트 → 에디터 확인 필요 시 사용자에게 요청 | 검증 결과 |
| **6. 기록** | 영향 문서 갱신, ADR, 커밋 메시지 | 갱신된 `docs/` |

**보고 규칙**: 검증하지 않은 것을 "동작한다"고 말하지 않는다. 실행하지 못한 단계(예: 에디터가 열려 있어 batchmode 실패)는 그대로 보고한다.

---

## 3. 역할 프로필

한 세션 안에서도 작업 성격에 따라 아래 모드를 의식적으로 전환한다.
서브에이전트를 쓸 수 있는 환경이면 각 프로필을 그대로 위임 프롬프트로 사용해도 된다.

### 3.1 Designer (설계)
- 입력: `docs/project/*`
- 산출: 기능 스펙, 수용 기준(Acceptance Criteria), `docs/project/roadmap.md` 태스크 갱신
- 금지: 밸런스 수치·핵심 룰의 독단 결정

### 3.2 Architect (구조)
- 입력: `docs/architecture/*`
- 산출: 모듈 경계, 어셈블리 구성, 데이터 흐름, **ADR**
- 체크: 어셈블리 순환 참조 없음 / 씬 간 결합 최소 / 네트워크 권위 명시

### 3.3 Implementer (구현)
- 입력: `docs/conventions/*` + 대상 코드
- 산출: C# 코드, 프리팹 배선 지침(에디터 작업은 사람이 수행)
- 체크: 하드 룰 준수, 할당 없는 핫패스, null 안전, 서버 권위

### 3.4 Reviewer (검토)
- 입력: diff + `docs/conventions/*`
- 체크리스트: [docs/workflow/development-loop.md](docs/workflow/development-loop.md)의 완료 기준
- 산출: 심각도별 지적(Blocker / Major / Minor)

### 3.5 Scribe (기록)
- 트리거: 구조 변경, 규약 변경, 기술 선택, 마일스톤 완료
- 산출: `docs/` 갱신, ADR, 로드맵 상태 변경

---

## 4. 이 저장소에서 특히 조심할 것 (Unity 고유)

| 위험 | 규칙 |
| --- | --- |
| GUID 파손 | `.meta` 직접 편집/삭제 금지. 파일 이동 시 `.meta` 동반 이동 |
| 씬/프리팹 손상 | `.unity`, `.prefab`, `.asset` 직접 텍스트 편집 금지(승인 시 예외) |
| 에디터 잠금 | Unity 에디터 실행 중에는 `-batchmode` 명령 실패. 먼저 종료 요청 |
| 생성물 오염 | `Library/`, `Temp/`, `Logs/`, `UserSettings/`, `*.csproj`, `*.sln*` 커밋 금지 |
| 프리팹 배선 | **MCP 연결 시** 직접 수행 가능. 미연결 시 단계별 지시로 위임 → [docs/workflow/unity-mcp.md](docs/workflow/unity-mcp.md) |
| MCP 오남용 | 도구가 가능해도 허가는 아니다. 씬·에셋 변경 전 고지, 삭제·일괄 변경은 승인 후, 저장 여부 보고 |
| 네트워크 오동작 | 클라이언트 권위 로직 금지. `IsServer`/`IsOwner` 분기를 명시 |
| 플랫폼 종속 확산 | `Steamworks` 참조는 Steam 레이어에만. Gameplay·UI는 `ISteamLobbyService` 경유 |
| Steam 검증 불가 | Steam 2인 접속은 **PC 2대 + 계정 2개** 필요. 단일 PC 검증 결과를 "동작한다"로 보고하지 않는다 |

---

## 5. 커뮤니케이션 규약

- 언어: **한국어**. 단, 코드 식별자·커밋 메시지 타입·기술 용어는 영문 유지.
- 파일 참조는 경로로 표기한다: `Assets/Scripts/Gameplay/Player/PlayerMotor.cs:42`
- 추측과 사실을 구분해 말한다. 확인하지 못한 것은 "확인 필요"로 표시한다.
- 사용자 액션이 필요하면 **번호 매긴 지시**로 준다 (예: 프리팹 배선, 에디터 설정).

---

## 6. 에스컬레이션 — 반드시 사람에게 물을 것

- 게임 디자인 결정(룰, 밸런스, 시스템 추가/삭제)
- 패키지·Unity 버전·URP 설정 변경
- 폴더 대규모 이동, 어셈블리 재구성
- `git push`, force update, 커밋 되돌리기 등 원격/이력 조작
- 문서와 코드가 충돌하는데 어느 쪽이 옳은지 불명확할 때
